feat: add AbortController to prevent redundant fetches on re-renders

Implemented AbortController to cancel ongoing fetch requests when the component re-renders or unmounts. This reduces the number of unnecessary network calls and improves performance by using controller.abort() in the cleanup function.



